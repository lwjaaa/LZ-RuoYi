请优化一下宽度调节器，并给整个index文件的出修改后的代码。我希望改得像语雀一模一样。

1. 并且当鼠标悬浮的时候，中间显示【展开/收起】的按钮。点击收起左侧标签面板宽度设置为0px。点击展开，左侧标签面板设置为280px。
2. 当通过拖拽调节器，可以调整左侧标签面板宽度，和右侧内筒区域宽度。调节到左侧宽度小于280px时，会触发左侧面板的收起事件，右侧面板宽度最大。大于280px时触左侧面板的展开事件，并继续随拖拽而调节宽度。
3. 调节器的可见宽度只要1px；